# Python bytecode 2.7 (decompiled from Python 2.7)
# Embedded file name: scripts/common/site-packages/future-0.18.2/future/backports/email/message.py
from __future__ import absolute_import, division, unicode_literals
from future.builtins import list, range, str, zip
__all__ = [u'Message']
import re
import uu
import base64
import binascii
from io import BytesIO, StringIO
from future.utils import as_native_str
from future.backports.email import utils
from future.backports.email import errors
from future.backports.email._policybase import compat32
from future.backports.email import charset as _charset
from future.backports.email._encoded_words import decode_b
Charset = _charset.Charset
SEMISPACE = u'; '
tspecials = re.compile(u'[ \\(\\)<>@,;:\\\\"/\\[\\]\\?=]')

def _splitparam(param):
    a, sep, b = str(param).partition(u';')
    return (a.strip(), None) if not sep else (a.strip(), b.strip())


def _formatparam(param, value=None, quote=True):
    if value is not None and len(value) > 0:
        if isinstance(value, tuple):
            param += u'*'
            value = utils.encode_rfc2231(value[2], value[0], value[1])
            return u'%s=%s' % (param, value)
        else:
            try:
                value.encode(u'ascii')
            except UnicodeEncodeError:
                param += u'*'
                value = utils.encode_rfc2231(value, u'utf-8', u'')
                return u'%s=%s' % (param, value)

            if quote or tspecials.search(value):
                return u'%s="%s"' % (param, utils.quote(value))
            return u'%s=%s' % (param, value)
    else:
        return param
    return


def _parseparam(s):
    s = u';' + str(s)
    plist = []
    while s[:1] == u';':
        s = s[1:]
        end = s.find(u';')
        while end > 0 and (s.count(u'"', 0, end) - s.count(u'\\"', 0, end)) % 2:
            end = s.find(u';', end + 1)

        if end < 0:
            end = len(s)
        f = s[:end]
        if u'=' in f:
            i = f.index(u'=')
            f = f[:i].strip().lower() + u'=' + f[i + 1:].strip()
        plist.append(f.strip())
        s = s[end:]

    return plist


def _unquotevalue(value):
    if isinstance(value, tuple):
        return (value[0], value[1], utils.unquote(value[2]))
    else:
        return utils.unquote(value)


class Message(object):

    def __init__(self, policy=compat32):
        self.policy = policy
        self._headers = list()
        self._unixfrom = None
        self._payload = None
        self._charset = None
        self.preamble = self.epilogue = None
        self.defects = []
        self._default_type = u'text/plain'
        return

    @as_native_str(encoding=u'utf-8')
    def __str__(self):
        return self.as_string()

    def as_string(self, unixfrom=False, maxheaderlen=0):
        from future.backports.email.generator import Generator
        fp = StringIO()
        g = Generator(fp, mangle_from_=False, maxheaderlen=maxheaderlen)
        g.flatten(self, unixfrom=unixfrom)
        return fp.getvalue()

    def is_multipart(self):
        return isinstance(self._payload, list)

    def set_unixfrom(self, unixfrom):
        self._unixfrom = unixfrom

    def get_unixfrom(self):
        return self._unixfrom

    def attach(self, payload):
        if self._payload is None:
            self._payload = [payload]
        else:
            self._payload.append(payload)
        return

    def get_payload(self, i=None, decode=False):
        if self.is_multipart():
            if decode:
                return
            elif i is None:
                return self._payload
            else:
                return self._payload[i]
        if i is not None and not isinstance(self._payload, list):
            raise TypeError(u'Expected list, got %s' % type(self._payload))
        payload = self._payload
        cte = str(self.get(u'content-transfer-encoding', u'')).lower()
        if isinstance(payload, str):
            payload = str(payload)
            if utils._has_surrogates(payload):
                bpayload = payload.encode(u'ascii', u'surrogateescape')
                if not decode:
                    try:
                        payload = bpayload.decode(self.get_param(u'charset', u'ascii'), u'replace')
                    except LookupError:
                        payload = bpayload.decode(u'ascii', u'replace')

            elif decode:
                try:
                    bpayload = payload.encode(u'ascii')
                except UnicodeError:
                    bpayload = payload.encode(u'raw-unicode-escape')

        if not decode:
            return payload
        elif cte == u'quoted-printable':
            return utils._qdecode(bpayload)
        elif cte == u'base64':
            value, defects = decode_b(''.join(bpayload.splitlines()))
            for defect in defects:
                self.policy.handle_defect(self, defect)

            return value
        else:
            if cte in (u'x-uuencode', u'uuencode', u'uue', u'x-uue'):
                in_file = BytesIO(bpayload)
                out_file = BytesIO()
                try:
                    uu.decode(in_file, out_file, quiet=True)
                    return out_file.getvalue()
                except uu.Error:
                    return bpayload

            return bpayload if isinstance(payload, str) else payload

    def set_payload(self, payload, charset=None):
        self._payload = payload
        if charset is not None:
            self.set_charset(charset)
        return

    def set_charset(self, charset):
        if charset is None:
            self.del_param(u'charset')
            self._charset = None
            return
        else:
            if not isinstance(charset, Charset):
                charset = Charset(charset)
            self._charset = charset
            if u'MIME-Version' not in self:
                self.add_header(u'MIME-Version', u'1.0')
            if u'Content-Type' not in self:
                self.add_header(u'Content-Type', u'text/plain', charset=charset.get_output_charset())
            else:
                self.set_param(u'charset', charset.get_output_charset())
            if charset != charset.get_output_charset():
                self._payload = charset.body_encode(self._payload)
            if u'Content-Transfer-Encoding' not in self:
                cte = charset.get_body_encoding()
                try:
                    cte(self)
                except TypeError:
                    self._payload = charset.body_encode(self._payload)
                    self.add_header(u'Content-Transfer-Encoding', cte)

            return

    def get_charset(self):
        return self._charset

    def __len__(self):
        return len(self._headers)

    def __getitem__(self, name):
        return self.get(name)

    def __setitem__(self, name, val):
        max_count = self.policy.header_max_count(name)
        if max_count:
            lname = name.lower()
            found = 0
            for k, v in self._headers:
                if k.lower() == lname:
                    found += 1
                    if found >= max_count:
                        raise ValueError(u'There may be at most {} {} headers in a message'.format(max_count, name))

        self._headers.append(self.policy.header_store_parse(name, val))

    def __delitem__(self, name):
        name = name.lower()
        newheaders = list()
        for k, v in self._headers:
            if k.lower() != name:
                newheaders.append((k, v))

        self._headers = newheaders

    def __contains__(self, name):
        return name.lower() in [ k.lower() for k, v in self._headers ]

    def __iter__(self):
        for field, value in self._headers:
            yield field

    def keys(self):
        return [ k for k, v in self._headers ]

    def values(self):
        return [ self.policy.header_fetch_parse(k, v) for k, v in self._headers ]

    def items(self):
        return [ (k, self.policy.header_fetch_parse(k, v)) for k, v in self._headers ]

    def get(self, name, failobj=None):
        name = name.lower()
        for k, v in self._headers:
            if k.lower() == name:
                return self.policy.header_fetch_parse(k, v)

        return failobj

    def set_raw(self, name, value):
        self._headers.append((name, value))

    def raw_items(self):
        return iter(self._headers.copy())

    def get_all(self, name, failobj=None):
        values = []
        name = name.lower()
        for k, v in self._headers:
            if k.lower() == name:
                values.append(self.policy.header_fetch_parse(k, v))

        return failobj if not values else values

    def add_header(self, _name, _value, **_params):
        parts = []
        for k, v in _params.items():
            if v is None:
                parts.append(k.replace(u'_', u'-'))
            parts.append(_formatparam(k.replace(u'_', u'-'), v))

        if _value is not None:
            parts.insert(0, _value)
        self[_name] = SEMISPACE.join(parts)
        return

    def replace_header(self, _name, _value):
        _name = _name.lower()
        for i, (k, v) in zip(range(len(self._headers)), self._headers):
            if k.lower() == _name:
                self._headers[i] = self.policy.header_store_parse(k, _value)
                break
        else:
            raise KeyError(_name)

    def get_content_type(self):
        missing = object()
        value = self.get(u'content-type', missing)
        if value is missing:
            return self.get_default_type()
        ctype = _splitparam(value)[0].lower()
        return u'text/plain' if ctype.count(u'/') != 1 else ctype

    def get_content_maintype(self):
        ctype = self.get_content_type()
        return ctype.split(u'/')[0]

    def get_content_subtype(self):
        ctype = self.get_content_type()
        return ctype.split(u'/')[1]

    def get_default_type(self):
        return self._default_type

    def set_default_type(self, ctype):
        self._default_type = ctype

    def _get_params_preserve(self, failobj, header):
        missing = object()
        value = self.get(header, missing)
        if value is missing:
            return failobj
        params = []
        for p in _parseparam(value):
            try:
                name, val = p.split(u'=', 1)
                name = name.strip()
                val = val.strip()
            except ValueError:
                name = p.strip()
                val = u''

            params.append((name, val))

        params = utils.decode_params(params)
        return params

    def get_params(self, failobj=None, header=u'content-type', unquote=True):
        missing = object()
        params = self._get_params_preserve(missing, header)
        if params is missing:
            return failobj
        elif unquote:
            return [ (k, _unquotevalue(v)) for k, v in params ]
        else:
            return params

    def get_param(self, param, failobj=None, header=u'content-type', unquote=True):
        if header not in self:
            return failobj
        for k, v in self._get_params_preserve(failobj, header):
            if k.lower() == param.lower():
                if unquote:
                    return _unquotevalue(v)
                else:
                    return v

        return failobj

    def set_param(self, param, value, header=u'Content-Type', requote=True, charset=None, language=u''):
        if not isinstance(value, tuple) and charset:
            value = (charset, language, value)
        if header not in self and header.lower() == u'content-type':
            ctype = u'text/plain'
        else:
            ctype = self.get(header)
        if not self.get_param(param, header=header):
            if not ctype:
                ctype = _formatparam(param, value, requote)
            else:
                ctype = SEMISPACE.join([ctype, _formatparam(param, value, requote)])
        else:
            ctype = u''
            for old_param, old_value in self.get_params(header=header, unquote=requote):
                append_param = u''
                if old_param.lower() == param.lower():
                    append_param = _formatparam(param, value, requote)
                else:
                    append_param = _formatparam(old_param, old_value, requote)
                if not ctype:
                    ctype = append_param
                ctype = SEMISPACE.join([ctype, append_param])

        if ctype != self.get(header):
            del self[header]
            self[header] = ctype

    def del_param(self, param, header=u'content-type', requote=True):
        if header not in self:
            return
        new_ctype = u''
        for p, v in self.get_params(header=header, unquote=requote):
            if p.lower() != param.lower():
                if not new_ctype:
                    new_ctype = _formatparam(p, v, requote)
                else:
                    new_ctype = SEMISPACE.join([new_ctype, _formatparam(p, v, requote)])

        if new_ctype != self.get(header):
            del self[header]
            self[header] = new_ctype

    def set_type(self, type, header=u'Content-Type', requote=True):
        if not type.count(u'/') == 1:
            raise ValueError
        if header.lower() == u'content-type':
            del self[u'mime-version']
            self[u'MIME-Version'] = u'1.0'
        if header not in self:
            self[header] = type
            return
        params = self.get_params(header=header, unquote=requote)
        del self[header]
        self[header] = type
        for p, v in params[1:]:
            self.set_param(p, v, header, requote)

    def get_filename(self, failobj=None):
        missing = object()
        filename = self.get_param(u'filename', missing, u'content-disposition')
        if filename is missing:
            filename = self.get_param(u'name', missing, u'content-type')
        return failobj if filename is missing else utils.collapse_rfc2231_value(filename).strip()

    def get_boundary(self, failobj=None):
        missing = object()
        boundary = self.get_param(u'boundary', missing)
        return failobj if boundary is missing else utils.collapse_rfc2231_value(boundary).rstrip()

    def set_boundary(self, boundary):
        missing = object()
        params = self._get_params_preserve(missing, u'content-type')
        if params is missing:
            raise errors.HeaderParseError(u'No Content-Type header found')
        newparams = list()
        foundp = False
        for pk, pv in params:
            if pk.lower() == u'boundary':
                newparams.append((u'boundary', u'"%s"' % boundary))
                foundp = True
            newparams.append((pk, pv))

        if not foundp:
            newparams.append((u'boundary', u'"%s"' % boundary))
        newheaders = list()
        for h, v in self._headers:
            if h.lower() == u'content-type':
                parts = list()
                for k, v in newparams:
                    if v == u'':
                        parts.append(k)
                    parts.append(u'%s=%s' % (k, v))

                val = SEMISPACE.join(parts)
                newheaders.append(self.policy.header_store_parse(h, val))
            newheaders.append((h, v))

        self._headers = newheaders

    def get_content_charset(self, failobj=None):
        missing = object()
        charset = self.get_param(u'charset', missing)
        if charset is missing:
            return failobj
        if isinstance(charset, tuple):
            pcharset = charset[0] or u'us-ascii'
            try:
                as_bytes = charset[2].encode(u'raw-unicode-escape')
                charset = str(as_bytes, pcharset)
            except (LookupError, UnicodeError):
                charset = charset[2]

        try:
            charset.encode(u'us-ascii')
        except UnicodeError:
            return failobj

        return charset.lower()

    def get_charsets(self, failobj=None):
        return [ part.get_content_charset(failobj) for part in self.walk() ]

    from future.backports.email.iterators import walk
