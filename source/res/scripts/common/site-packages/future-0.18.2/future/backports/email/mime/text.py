# Python bytecode 2.7 (decompiled from Python 2.7)
# Embedded file name: scripts/common/site-packages/future-0.18.2/future/backports/email/mime/text.py
from __future__ import unicode_literals
from __future__ import division
from __future__ import absolute_import
__all__ = [u'MIMEText']
from future.backports.email.encoders import encode_7or8bit
from future.backports.email.mime.nonmultipart import MIMENonMultipart

class MIMEText(MIMENonMultipart):

    def __init__(self, _text, _subtype=u'plain', _charset=None):
        if _charset is None:
            try:
                _text.encode(u'us-ascii')
                _charset = u'us-ascii'
            except UnicodeEncodeError:
                _charset = u'utf-8'

        MIMENonMultipart.__init__(self, u'text', _subtype, **{u'charset': _charset})
        self.set_payload(_text, _charset)
        return
