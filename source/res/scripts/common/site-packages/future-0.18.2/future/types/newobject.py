# Python bytecode 2.7 (decompiled from Python 2.7)
# Embedded file name: scripts/common/site-packages/future-0.18.2/future/types/newobject.py


class newobject(object):

    def next(self):
        if hasattr(self, '__next__'):
            return type(self).__next__(self)
        raise TypeError('newobject is not an iterator')

    def __unicode__(self):
        if hasattr(self, '__str__'):
            s = type(self).__str__(self)
        else:
            s = str(self)
        if isinstance(s, unicode):
            return s
        else:
            return s.decode('utf-8')

    def __nonzero__(self):
        if hasattr(self, '__bool__'):
            return type(self).__bool__(self)
        return type(self).__len__(self) if hasattr(self, '__len__') else True

    def __long__(self):
        return NotImplemented if not hasattr(self, '__int__') else self.__int__()

    def __native__(self):
        return object(self)

    __slots__ = []


__all__ = ['newobject']
