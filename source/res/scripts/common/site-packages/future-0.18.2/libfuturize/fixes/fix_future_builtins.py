# Python bytecode 2.7 (decompiled from Python 2.7)
# Embedded file name: scripts/common/site-packages/future-0.18.2/libfuturize/fixes/fix_future_builtins.py
from __future__ import unicode_literals
from lib2to3 import fixer_base
from lib2to3.pygram import python_symbols as syms
from lib2to3.fixer_util import Name, Call, in_special_context
from libfuturize.fixer_util import touch_import_top
replaced_builtin_fns = u'filter map zip\n                       ascii chr hex input next oct\n                       bytes range str raw_input'.split()
expression = u'|'.join([ u"name='{0}'".format(name) for name in replaced_builtin_fns ])

class FixFutureBuiltins(fixer_base.BaseFix):
    BM_compatible = True
    run_order = 7
    PATTERN = u"\n              power<\n                 ({0}) trailer< '(' [arglist=any] ')' >\n              rest=any* >\n              |\n              power<\n                  'map' trailer< '(' [arglist=any] ')' >\n              >\n              ".format(expression)

    def transform(self, node, results):
        name = results[u'name']
        touch_import_top(u'builtins', name.value, node)
