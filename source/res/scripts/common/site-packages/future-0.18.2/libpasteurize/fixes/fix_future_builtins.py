# Python bytecode 2.7 (decompiled from Python 2.7)
# Embedded file name: scripts/common/site-packages/future-0.18.2/libpasteurize/fixes/fix_future_builtins.py
from __future__ import unicode_literals
from lib2to3 import fixer_base
from lib2to3.pygram import python_symbols as syms
from lib2to3.fixer_util import Name, Call, in_special_context
from libfuturize.fixer_util import touch_import_top
replaced_builtins = u'filter map zip\n                       ascii chr hex input next oct open round super\n                       bytes dict int range str'.split()
expression = u'|'.join([ u"name='{0}'".format(name) for name in replaced_builtins ])

class FixFutureBuiltins(fixer_base.BaseFix):
    BM_compatible = True
    run_order = 9
    PATTERN = u"\n              power<\n                 ({0}) trailer< '(' args=[any] ')' >\n              rest=any* >\n              ".format(expression)

    def transform(self, node, results):
        name = results[u'name']
        touch_import_top(u'builtins', name.value, node)
