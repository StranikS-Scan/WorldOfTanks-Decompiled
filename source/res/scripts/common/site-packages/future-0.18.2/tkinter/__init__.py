# Python bytecode 2.7 (decompiled from Python 2.7)
# Embedded file name: scripts/common/site-packages/future-0.18.2/tkinter/__init__.py
from __future__ import absolute_import
import sys
if sys.version_info[0] < 3:
    from Tkinter import *
    from Tkinter import _cnfmerge, _default_root, _flatten, _support_default_root, _test, _tkinter, _setit
    try:
        from Tkinter import _join
    except ImportError:
        pass

    try:
        from Tkinter import _stringify
    except ImportError:
        pass

    try:
        from Tkinter import _splitdict
    except ImportError:
        pass

else:
    raise ImportError('This package should not be accessible on Python 3. Either you are trying to run from the python-future src folder or your installation of python-future is corrupted.')
