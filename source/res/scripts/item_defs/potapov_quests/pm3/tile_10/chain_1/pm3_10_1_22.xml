<pm3_10_1_22.xml>
  <quests>
    <potapovQuest>
      <enabled>	true	</enabled>
      <id>	pm3_10_1_22_main	</id>
      <serverOnly>	true	</serverOnly>
      <conditions></conditions>
      <scripts>	
LOG = globals().get('LOG')
""":type: typing.LOG_DEBUG_DEV"""

QUEST_EVENT_TYPE = globals().get('QUEST_EVENT_TYPE')
""":type: quest_definitions.QUEST_EVENT_TYPE"""

quest_progress = globals().get('quest_progress')
""":type: quest_progress"""

MAIN_PROGRESS_SERIES = 'battlesSeries'
MAIN_PROGRESS_TOP_BY_EXP = 'topByExp'

def onInit(eventType, eventData, quest, processor):
    for event in events.iterkeys():
        processor.subscribe(event, quest)
    return True

def onFinishBattle(eventType, eventData, quest, processor):
    questProgress = quest_progress.BaseQuestProgress(quest.config, processor.getQuestProgress(quest.progressID))

    topByExp = eventData['avatarResults']['misc']['total']['xp']
    topByExpRes = topByExp &lt;= questProgress.getProgress(MAIN_PROGRESS_TOP_BY_EXP).getParam("desiredPosition")

    if topByExp:
        questProgress.increaseUntilComplete(MAIN_PROGRESS_SERIES, 1)

    eventData['results'].update(processor.checkComplete(questProgress, quest.progressID))
    processor.setQuestProgress(quest.progressID, questProgress.save())

events = {
    QUEST_EVENT_TYPE.INIT: onInit,
    QUEST_EVENT_TYPE.FINISH_BATTLE: onFinishBattle,
}

def quest(eventType, eventData, quest, processor):
    LOG('QUEST EVENT:', eventType, quest.id)
    return events.get(eventType, lambda *args: False)(eventType, eventData, quest, processor)
         	</scripts>
      <bonus>
        <freeXP>	2000	</freeXP>
        <credits>	400000	</credits>
        <token>
          <id>	token:pt:final:s3:t10:main	</id>
          <expires>	28.01.2100 00:01	</expires>
          <count>	1	</count>
        </token>
      </bonus>
    </potapovQuest>
  </quests>
</pm3_10_1_22.xml>
